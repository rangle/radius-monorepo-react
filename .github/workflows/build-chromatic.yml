# Workflow name
name: build-chromatic

# Event for the workflow
on:
  pull_request:
    branches:
      - main
#  push:
#    branches:
#      - main

# List of jobs
jobs:
  chromatic-deployment:
    # Operating System
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    # Job steps
    steps:
      - name: Checkout the codebase
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            ~/.cache
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      # - name: Validate tokens
      #   id: validate-tokens
      #   working-directory: library/foundations
      #   run: |
      #     # Parse and Validate Design Tokens
      #     yarn tokens:ci

      # - name: Lint
      #   run: yarn lint:ci

      - name: Build projects 🏗
        run: yarn build

      # - name: Test
      #   run: yarn test -- --coverage

      # - name: Coverage Report
      #   uses: vebr/jest-lcov-reporter@v0.2.0
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     lcov-file: ./coverage/lcov.info

      # 👇 Adds Chromatic as a step in the workflow
      - name: Publish to Chromatic
        uses: chromaui/action@v1
        # Chromatic GitHub Action options
        with:
          # 👇 Chromatic projectToken, refer to the manage page to obtain it.
          buildScriptName: build-storybook
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}

      - name: Publish Summary
        run: echo -e "| Results |  |\n| --- | --- |\n| Build Results | ${{steps.chromatic.outputs.buildUrl}} |\n| Storybook Preview | ${{steps.chromatic.outputs.storybookUrl}} |\n| Component Count | ${{steps.chromatic.outputs.componentCount}} |" >> $GITHUB_STEP_SUMMARY

      # # This checks if there's an existing comment in the PR with the Chromatic link, and either updates the existing comment or creates a new one.
      # - name: Create PR comment with Chromatic link
      #   if: steps.chromatic.outputs.url != ''
      #   uses: actions/github-script@v5
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const chromaticUrl = '${{ steps.chromatic.outputs.url }}';
      #       const prComment = `:art: [Chromatic preview](${chromaticUrl}) :art:`;

      #       const { data: pullRequest } = await github.pulls.get({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         pull_number: context.issue.number,
      #       });

      #       const comments = await github.pulls.listReviewComments({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         pull_number: context.issue.number,
      #       });

      #       const botComment = comments.data.find(
      #         (comment) => comment.user.login === 'github-actions[bot]' && comment.body.includes('Chromatic preview')
      #       );

      #       if (botComment) {
      #         await github.pulls.updateReviewComment({
      #           owner: context.repo.owner,
      #           repo: context.repo.repo,
      #           comment_id: botComment.id,
      #           body: prComment,
      #         });
      #       } else {
      #         await github.pulls.createReviewComment({
      #           owner: context.repo.owner,
      #           repo: context.repo.repo,
      #           pull_number: context.issue.number,
      #           commit_id: pullRequest.head.sha,
      #           body: prComment,
      #           path: '', // You can specify a path if you want the comment to appear on a specific file
      #           position: null, // You can specify a position if you want the comment to appear on a specific line
      #         });
      #       }
