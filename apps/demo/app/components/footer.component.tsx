import React, { FC } from 'react';
import { RadiusAutoLayout } from '@rangle/radius-react-core-components';

type FooterProps = {};

export const Footer: FC<FooterProps> = (props) => (
  <>
    <RadiusAutoLayout direction="horizontal">
      Footer Placeholder
    </RadiusAutoLayout>
  </>
);
