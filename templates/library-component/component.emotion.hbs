import React, { FC, PropsWithChildren, ComponentPropsWithoutRef, useMemo } from 'react';

import { getStyles } from './{{dashCase name}}.styles';

export type {{pascalCase prefix}}{{pascalCase name}}Props = PropsWithChildren<{
  // exampleProp: unknown;
}> & ComponentPropsWithoutRef<'{{lowerCase elementType}}'>;

export const {{pascalCase prefix}}{{pascalCase name}} : FC<{{pascalCase prefix}}{{pascalCase name}}Props> = ({ children, /* exampleProp, */ ...rest }) => {
  const className = useMemo(
    () =>
      getStyles({
        /* exampleProp */
      }),
    [
      /* exampleProp */
    ]
  );
  return (
    <{{lowerCase elementType}} className={className} {...rest}>
      {children}
    </{{lowerCase elementType}}>
  );
};