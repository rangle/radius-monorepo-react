import React from 'react';
import { ComponentMeta, ComponentStory } from '@storybook/react';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { {{pascalCase prefix}}{{pascalCase name}}, {{pascalCase prefix}}{{pascalCase name}}Props } from './{{dashCase name}}';

export default {
  component: {{pascalCase prefix}}{{pascalCase name}},
  title: 'Components/{{pascalCase name}}',
  parameters: {
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/????',
    },
    badges: [BADGE.EXPERIMENTAL],
  },
} as ComponentMeta<typeof {{pascalCase prefix}}{{pascalCase name}}>;

const Template: ComponentStory<typeof {{pascalCase prefix}}{{pascalCase name}}> = (
  args: {{pascalCase prefix}}{{pascalCase name}}Props
) => <{{pascalCase prefix}}{{pascalCase name}} {...args}>Hello World</{{pascalCase prefix}}{{pascalCase name}}>;

export const Default = Template.bind({});
Default.args = {

};